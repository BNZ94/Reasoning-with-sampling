base_model_name: "Qwen/Qwen2.5-Math-1.5B-Instruct"

dataset_path: "data/sample_math.jsonl"
n_questions: 50

methods:
  - power_mcmc

nmcmc_sweep: [10]

alpha: 4.0
B: 192
T_max: 512      # mets 1024 si tu as la VRAM / le temps
top_p: 1.0
do_sample: true

seed: 42
save_outputs: false
verbose: false

device: null
dtype: "bfloat16"
